# Current State

**Last completed stage:** Stage 10 (Final Testing, Security, Observability & Submission Audit)
**Current stage:** FINAL — Submission Ready

## Implemented

### Infrastructure (Stage 1)
- [x] Next.js App Router + NestJS modular monolith
- [x] PostgreSQL + TypeORM, migrations only (8 migrations); Docker runs pending migrations before API startup
- [x] Redis client + BullMQ notification queue and processor
- [x] MinIO private attachment storage with presigned upload/download
- [x] Docker Compose, all 5 services healthchecked
- [x] Health (`/api/health`, `/api/health/live`), Prometheus metrics (`/api/metrics`), pino logging, `x-correlation-id`
- [x] Swagger (`/api/docs`), CI (GitHub Actions: backend lint/build/unit/e2e with real Postgres, frontend lint/test/build)

### Authentication (Stage 2)
- [x] Register / Login (argon2id password hashing)
- [x] Access JWT (15 min, `{sub, email}` payload only), returned in body
- [x] Refresh token: opaque random, HMAC-hashed at rest, httpOnly cookie (`refresh_token`, path `/`)
- [x] Refresh rotation + reuse detection (reuse revokes all sessions for that user)
- [x] Logout (revokes session, clears cookie)
- [x] `GET /api/auth/me`
- [x] Login rate-limited (5/60s), register rate-limited (20/60s) — separate buckets, verified not to leak onto other routes

### Workspaces
- [x] Create workspace (transactional: workspace + OWNER membership together)
- [x] List my workspaces, get one workspace (404 for non-members)
- [x] Unique slug generation with collision retry

### RBAC
- [x] `OWNER > ADMIN > EDITOR > VIEWER`, enforced via `WorkspacePermissionsService`
- [x] Owner immutable: cannot be removed, demoted, or leave (Stage 2 policy — no ownership transfer yet)
- [x] Member list, role change (`PATCH .../members/:id`), remove (`DELETE .../members/:id`), self-leave (`DELETE .../members/me`)

### Invitations
- [x] Create (OWNER/ADMIN only, role restricted to ADMIN/EDITOR/VIEWER)
- [x] Token hashed at rest (SHA-256); raw token/URL only returned in the API response when `NODE_ENV !== production`
- [x] List for workspace (OWNER/ADMIN), list for current user's email (`/invitations/me`)
- [x] Accept/reject by token (`/invitations/:token/...`, email-link flow) **and** by id (`/invitations/by-id/:id/...`, in-app flow used by the dashboard since the raw token can't be recovered after hashing)
- [x] Concurrency-safe accept (row lock + transaction), partial unique DB index prevents duplicate active invites

### Documents (Stage 3)
- [x] `Document` entity (workspace-scoped, self-referencing `parentId`, `position` double precision, `archivedAt` soft-delete)
- [x] CRUD: create (root or child), list (flat, `includeArchived` query param), get, rename (`PATCH`)
- [x] Move/reorder via `POST .../move` — `parentId` (null = root) + optional `referenceId`/`placement` (`before`/`after`) for midpoint-bisection sibling reordering
- [x] Tree invariants enforced server-side: self-parent rejected, cycle rejected (walks up the candidate parent's ancestor chain), cross-workspace parent rejected (same scoped-lookup that also gives IDOR protection)
- [x] Archive/restore act on the **entire subtree** (BFS-collected), not just the target node — see ADR-011
- [x] Restoring a node whose parent is still archived (or gone) reparents it to workspace root instead of leaving it orphaned/invisible
- [x] Authorization reuses `WorkspacePermissionsService.canCreateDocument`/`canEditDocument` (VIEWER read-only, everyone else can mutate) and `WorkspaceMembershipGuard` (404-for-non-member policy) — no new permission system
- [x] IDOR protection: every lookup is scoped by `(id, workspaceId)` together (`getScopedWithManager`), never `findOne(id)` alone

### Realtime Collaboration (Stage 4)
- [x] `CollaborationGateway` (`backend/src/modules/collaboration/`) — socket.io gateway on the `/collab` namespace, one Yjs `Y.Doc` + `Awareness` per active document, held in-memory only (`CollaborationService`) - not persisted to Postgres, see ADR-013
- [x] Connection authorization pipeline before any document data is sent: JWT verified at handshake, then on `join` — workspace membership, document existence (workspace-scoped), and `canEditDocument` role check; a document is never trusted from the client without all three
- [x] Archived documents are read-only for every role via collaboration, matching the existing REST archive policy
- [x] CRDT sync: client sends `sync-update` (a Yjs update), server applies it (rejecting if `!canEdit`) and relays the identical bytes to the rest of the room - no "save whole document" endpoint, no `PATCH content`
- [x] Presence via `y-protocols/awareness`: `awareness-update` messages relayed through the same per-document `Awareness` instance; join/leave/cursor changes broadcast to the room; disconnect cleanup removes exactly that socket's awareness states
- [x] Reconnect/resync: client keeps its local `Y.Doc`/`Awareness` across a dropped socket; on reconnect it re-emits `join` and receives a fresh full-state `sync-update` - safe because Yjs update application is idempotent, so no history-tracking handshake is needed
- [x] Block content model (frontend, CRDT-backed): `Y.Array<Y.Map>` of blocks - paragraph, heading, bulletListItem, checkbox, codeBlock, image (metadata only, no upload) - each text-bearing block owns its own `Y.Text`
- [x] `CollaborativeEditor`/`BlockView`/`PresenceBar` (`frontend/src/features/collaboration/`) wired into `DocumentPage`, replacing the Stage 3 placeholder
- [x] Metrics: `collab_connections_current`, `collab_sessions_current` (gauges), `crdt_updates_total`, `collab_connection_errors_total{reason}` (counters, no per-document/user labels)

### Persistence & Version History (Stage 5)
- [x] `document_versions` table (`backend/src/modules/collaboration/entities/document-version.entity.ts`) — binary (`bytea`) full Yjs state snapshots, never JSON/plain text; `kind` discriminates `auto` (durability buffer)/`manual`/`restore-point` (history)
- [x] `CollaborationPersistenceService` — exactly one upserted `AUTO` row per document (partial unique index `(documentId) WHERE kind='auto'`), written via a trailing-throttled `scheduleFlush` (default 3s, `COLLAB_PERSIST_INTERVAL_MS` override) so continuous edits don't write a full snapshot per keystroke; safe against duplicate writes because Yjs update application is idempotent
- [x] Session hydration: `CollaborationGateway` loads the durable buffer into a brand-new in-memory session the first time a document is joined in a process (not on every join) - verified surviving an actual Docker container restart, not just an in-memory eviction
- [x] Conservative session eviction: `CollaborationService.scheduleEviction` (30s grace period, `COLLAB_EVICTION_GRACE_MS` override) flushes final state then drops the in-memory session only after zero connections for the whole grace period; a rejoin within the window cancels eviction and reuses the live session
- [x] Version history: list/inspect/create/restore (`workspaces/:workspaceId/documents/:documentId/versions...`), reusing `WorkspaceMembershipGuard`/`WorkspacePermissionsService.canEditDocument` - no second auth system
- [x] Restore semantics: current state is captured as a new `RESTORE_POINT` version **before** being overwritten (history is never destroyed); target content is applied via `replaceBlocksContent` (delete-all + reconstruct-and-insert inside one Yjs transaction, not a raw CRDT merge, since merges can only add operations, never remove them) and the resulting diff is broadcast live to the room, so already-connected clients converge without reloading
- [x] `VersionHistoryPanel` (frontend) — list/inspect/create/restore UI wired into `CollaborativeEditor`'s toolbar, with loading/empty/error states and an explicit restore confirmation step
- [x] Metrics: `collab_persist_total{result}`, `collab_versions_created_total{kind}`, `collab_version_restore_total{result}`, `collab_session_hydrated_total`, `collab_session_evicted_total`

### Realtime Collaboration (Stage 4)
- [x] `CollaborationGateway` (`backend/src/modules/collaboration/`) — socket.io gateway on the `/collab` namespace, one Yjs `Y.Doc` + `Awareness` per active document, held in-memory only (`CollaborationService`), backed by durable storage since Stage 5 (see above)
- [x] Connection authorization pipeline before any document data is sent: JWT verified at handshake, then on `join` — workspace membership, document existence (workspace-scoped), and `canEditDocument` role check; a document is never trusted from the client without all three
- [x] Archived documents are read-only for every role via collaboration, matching the existing REST archive policy
- [x] CRDT sync: client sends `sync-update` (a Yjs update), server applies it (rejecting if `!canEdit`) and relays the identical bytes to the rest of the room - no "save whole document" endpoint, no `PATCH content`
- [x] Presence via `y-protocols/awareness`: `awareness-update` messages relayed through the same per-document `Awareness` instance; join/leave/cursor changes broadcast to the room; disconnect cleanup removes exactly that socket's awareness states
- [x] Reconnect/resync: client keeps its local `Y.Doc`/`Awareness` across a dropped socket; on reconnect it re-emits `join` and receives a fresh full-state `sync-update` - safe because Yjs update application is idempotent, so no history-tracking handshake is needed
- [x] Block content model (frontend, CRDT-backed): `Y.Array<Y.Map>` of blocks - paragraph, heading, bulletListItem, checkbox, codeBlock, image (metadata only, no upload) - each text-bearing block owns its own `Y.Text`
- [x] `CollaborativeEditor`/`BlockView`/`PresenceBar` (`frontend/src/features/collaboration/`) wired into `DocumentPage`, replacing the Stage 3 placeholder
- [x] Metrics: `collab_connections_current`, `collab_sessions_current` (gauges), `crdt_updates_total`, `collab_connection_errors_total{reason}` (counters, no per-document/user labels)

### Comments, Mentions, Notifications & Attachments (Stage 6)
- [x] `Comment` entity (`backend/src/modules/comments/`) — document-scoped, two-level nesting only (root + flat replies, enforced in service logic not a DB constraint), soft-delete (`deletedAt`), `resolvedAt`/`resolvedById` for thread state, `editedAt` marker
- [x] CRUD + reply + resolve/reopen (`workspaces/:workspaceId/documents/:documentId/comments...`) — edit is author-only always; delete allows OWNER/ADMIN to moderate others' comments (`canModerateComments`); VIEWER is read-only (`canComment`/`assertCanComment` on `WorkspacePermissionsService`); archived documents block create/update/resolve/reopen but not delete (deliberate asymmetry — administrative cleanup stays possible)
- [x] `CommentMention` entity — `@user` mentions validated server-side against workspace membership (no cross-workspace mentions, 400 if invalid); duplicate mentions in one submission de-duped before insert (`Set` + unique composite index as DB-level backstop); editing a comment diffs old vs new mention sets and only notifies newly-added ones
- [x] `Notification` entity + `NotificationsService`/`NotificationsProcessor` — persistent in-app notifications for `mention`/`reply`/`thread_resolved`/`thread_reopened`, delivered async via a real BullMQ queue (`QueueName.NOTIFICATIONS`); idempotent by construction (`dedupeKey` unique column + `INSERT ... ON CONFLICT DO NOTHING`, with `jobId: dedupeKey` as a secondary BullMQ-level guard) — verified live that resolving an already-resolved thread twice does not duplicate a notification; list/unread-count/mark-one-read/mark-all-read (`/api/notifications...`, user-scoped not workspace-scoped, `JwtAuthGuard` only)
- [x] `Attachment` entity + `AttachmentsService` — MinIO direct-to-storage upload (presigned PUT), confirm step re-validates actual uploaded size via `statObject` (never trusts the client-declared size), size (20MB) and MIME allowlist enforced both at request time and confirm time, download via presigned GET, remove deletes both the MinIO object and the DB row; binaries never touch Postgres
- [x] Comment content is a failed-notification-enqueue-safe operation: `CommentsService.safeEnqueue` catches/logs enqueue failures rather than surfacing them as a failed comment request, since the comment DB transaction has already committed by that point (see the BullMQ colon-in-jobId bug below)
- [x] Frontend: `CommentsPanel` (list/reply/resolve/reopen/edit/delete + a mention picker that shows explicit `@member` chips rather than parsing free text), `NotificationsBell` (top-nav bell + unread badge, polls unread count every 20s, dropdown lists/marks-read), `AttachmentsPanel` (upload via file input → presigned PUT → confirm, list, open via presigned download URL, remove) — all wired into `CollaborativeEditor`'s toolbar alongside "History", following the same slide-over-panel shape as `VersionHistoryPanel`
- [x] Docker-deployment bug found and fixed: presigned upload/download URLs were signed against the internal Docker hostname (`MINIO_ENDPOINT=minio`), unreachable from a browser outside the Docker network. Fixed via a second `MinioService` client configured with `MINIO_PUBLIC_ENDPOINT`/`MINIO_PUBLIC_PORT` (defaults to the internal values when unset, so non-Docker local dev needs no change) — see ADR-016
- [x] Metrics: `comments_created_total{kind}`, `comment_threads_resolved_total{action}`, `notifications_processed_total{result}`, `notification_processing_failures_total`, `attachment_uploads_total{result}`

### Public Sharing, SSR/ISR & SEO (Stage 7)
- [x] `Document` gained `isPublished`/`publicSlug`/`publishedAt` columns (no new table - publishing is 1:1 with a document, same pattern as `archivedAt`). `publicSlug` is a plain unique index (Postgres allows multiple NULLs). Slug generation reuses `workspaces/slug.util.ts`'s `slugify`/`slugSuffix` collision-retry pattern
- [x] `POST .../documents/:id/publish` `{slug?}` / `POST .../documents/:id/unpublish` on the existing `DocumentsController` (no new controller/permission - reuses `assertCanEditDocument`, same boundary as every other document mutation: VIEWER blocked, EDITOR/ADMIN/OWNER allowed). Publishing is idempotent-on-republish (same slug reused unless a new one is requested); unpublishing is idempotent (no-op if already unpublished)
- [x] **Publishing model: "latest state"** (Model A) - the public page always reflects the document's current durable content; publish/unpublish only toggle visibility + slug, never snapshot content. Archiving a document (or any document in an archived subtree) automatically clears its publish state - archived + published can never coexist; publishing an already-archived document is rejected (400)
- [x] Public read path: `GET /api/public/documents/:slug` (`backend/src/modules/public/`, **zero auth guards** - the only such controller in the app) - reads the durable Yjs buffer via the existing `CollaborationPersistenceService.hydrate()` + `yjs-document.util.ts` decode/snapshot helpers, **never** an in-memory live collaboration session; returns only `{title, blocks, publishedAt}` - no workspace/document/user ids, no comments, no attachments, no version history
- [x] Public route `/p/[slug]` (frontend) - a plain **Server Component**, no `"use client"`, no editor reuse; fetches the backend's public API server-side with `next: {revalidate: 60, tags: [...]}` for ISR. `generateMetadata` sets title/description/canonical/Open Graph/Twitter tags; `robots.ts` allows `/` and `/p/`, disallows `/workspace/`, `/login`, `/register`, `/invitations/`
- [x] On-demand revalidation: publish/unpublish/republish/archive call `RevalidationService` → `POST {frontend}/api/revalidate` (shared-secret protected) → `revalidateTag(..., {expire: 0})` + `revalidatePath(...)`; the 60s ISR window is the fallback for ordinary content edits, which don't individually trigger a revalidation call (avoids coupling into the Yjs persistence throttle) — see ADR-017
- [x] Read-only renderer `PublicDocumentView` (`features/publishing/`) - a separate component from the editable `BlockView`, supports all Stage 4 block types (paragraph/heading/bulletListItem-grouped-into-`<ul>`/checkbox/codeBlock/image). All text renders as plain JSX children (React's automatic escaping is the XSS defense, verified live with an actual `<script>`/`onerror` payload - came back HTML-entity-escaped in the rendered page, confirmed via curl); `sanitizeUrl` allowlists `http`/`https` schemes for the one attribute-position value (image `src`)
- [x] Frontend sharing UX: `PublishControl` (`features/documents/`) - a small inline control (not a slide-over panel, since there's no list to browse) on the document page: Publish/Unpublish buttons, public URL + Copy link + Open, loading/error states
- [x] Two real bugs found via live Docker verification and fixed: (1) `RevalidationService`'s backend→frontend call used the browser-facing `FRONTEND_URL`, unreachable from inside the backend container - fixed with a new `FRONTEND_INTERNAL_URL` (Docker DNS), the same internal/public split pattern as Stage 6's MinIO fix; (2) the app's root `loading.tsx` implicitly wraps every route in a Suspense boundary, so `/p/[slug]`'s `notFound()` fired only after the response had already streamed a 200 status (documented Next.js 16 behavior, not fixable from the page itself) - fixed by checking document existence in `proxy.ts` before rendering starts, which now returns a real 404 pre-stream; see ADR-018
- [x] Metrics: `documents_published_total`, `documents_unpublished_total`, `public_render_failures_total`, `public_revalidation_failures_total`

### Search, Billing & Plan Limits (Stage 8)
- [x] **Search**: `documents.searchVector` (GENERATED ALWAYS tsvector, GIN-indexed, title weight A + content weight B) + `documents.contentText` (`select: false`). `GET .../documents/search?q=&limit=&offset=` — workspace-scoped, `archivedAt IS NULL`, `websearch_to_tsquery` (parameterized) + `ts_rank` ordering + `ts_headline` snippets; any workspace member (including VIEWER) can search, same bar as list/get; registered before `:documentId` in the controller so it isn't matched as a document id
- [x] Content indexing happens only from durable state: `CollaborationPersistenceService.flush()` writes `contentText` immediately after a successful durability-buffer write (decoding the just-persisted Yjs bytes), never from a live in-memory `Y.Doc` session — see ADR-019
- [x] Frontend: `SearchDialog` — Cmd/Ctrl+K command dialog (debounced 250ms, keyboard nav, loading/empty/error states, safe non-`dangerouslySetInnerHTML` snippet rendering), triggered from `WorkspaceSwitcher`
- [x] `Subscription` entity (1:1 per workspace, `plan`/`status`/`currentPeriodEnd`/provider fields), created transactionally alongside every new workspace; `BillingWebhookEvent` for idempotent webhook processing (unique `eventId`, same pattern as `Notification.dedupeKey`)
- [x] `EntitlementsService` — the single place plan limits/features are read from (`PLAN_LIMITS`: FREE 5 members/50 documents/100MB/no manual snapshots, PRO unlimited/unlimited/10GB/manual snapshots); `assertCanCreateDocument`/`assertCanInviteMember`/`assertCanUploadAttachment`/`assertFeatureEnabled`, all throwing a structured `PlanLimitExceededPayload` (`code: 'PLAN_LIMIT_EXCEEDED'`, `limitType`, `limit`, `current`, `plan`)
- [x] Entitlement checks are deliberately separate from `WorkspacePermissionsService` (role/authorization) — both must independently pass; billing mutations reuse the existing `assertCanManageWorkspaceSettings` (new, OWNER-only) rather than a new permission tier
- [x] Concurrency-safe hard limits: document-creation and member-invitation checks run inside a transaction that first locks the `Workspace` row (`EntitlementsService.lockWorkspace`), serializing concurrent requests at the exact limit boundary (verified live: 5 parallel document-creation requests at the last available slot → exactly one 201, four 403s). Attachment storage uses a lighter, documented count-then-check with no lock (accepted race window, uploads are already two-phase)
- [x] Manual version snapshots (`POST .../versions`) are the one gated premium feature (`featureEnabled(plan, 'manualVersionSnapshots')`) — restore and the automatic durability buffer are never gated
- [x] Mock payment provider (`PaymentProvider` interface + `PAYMENT_PROVIDER` token, `MockPaymentProvider`) — no real Stripe integration (not required by the original scope); `POST .../billing/mock-pay` is a dev-only stand-in for a real provider's async webhook, converging on the same idempotent `BillingService.applyEvent` core as the real webhook endpoint (`POST /billing/webhook`, shared-secret header, excluded from Swagger)
- [x] Downgrade (`POST .../billing/downgrade`) never deletes documents/members/attachments — only flips `plan`/`status`/`currentPeriodEnd`; existing resources stay fully readable/usable, and the same count-based entitlement checks simply block new creation until back under the limit or re-upgraded (safety by construction, not extra cleanup code — see ADR-020)
- [x] Frontend: `BillingSection` embedded in the existing workspace shell (not a new route) — plan/status/renewal, usage bars (members/documents/storage, `null` limit = "Unlimited"), Upgrade/Downgrade button gated by `canManageBilling` (OWNER-only); `PLAN_LIMIT_EXCEEDED` errors get an upgrade-path message instead of a generic failure (document creation, invitation accept)
- [x] Two real bugs found and fixed via testing (see ADR-020): `GlobalExceptionFilter` was silently stripping structured exception-body fields beyond `message`/`error`/`statusCode` (would have broken every future "upgrade CTA" use of a structured error, not just this one); `BillingService.applyEvent`'s duplicate-webhook check read `result.identifiers.length > 0`, but TypeORM's `orIgnore()` returns `[null]` (not `[]`) for a skipped conflicting row, so every duplicate delivery was silently treated as newly-applied
- [x] Metrics: `search_requests_total`, `search_failures_total`, `plan_limit_rejections_total{limit}`, `subscription_state_changes_total{result}`, `billing_webhook_failures_total`

### Frontend
- [x] `AuthProvider` (Client) — silent session bootstrap via refresh on mount, in-memory access token, `apiFetch` with 401-retry-once-via-refresh, `getAccessToken()` for the WS handshake (Stage 4)
- [x] `/login`, `/register` (Client forms, basic validation, `?reason=session-expired` banner on `/login` - Stage 9)
- [x] `/workspace` dashboard (list workspaces, create workspace, pending invitations with accept/reject)
- [x] `/workspace/[workspaceId]` shell (members list, role change/remove, invite form, billing section — all role-gated in the UI; backend re-enforces)
- [x] `/workspace/[workspaceId]/document/[documentId]` — document page shell: breadcrumb (ancestor chain), inline-editable title, archived banner + restore, real collaborative block editor with "Comments"/"Files"/"History" panels (Stage 5/6/9)
- [x] `WorkspaceDetailShell` (Client, Stage 9) — wraps `DocumentSidebar` + routed content; off-canvas drawer with a menu-button toggle below 768px instead of a fixed sidebar, so the editor never gets crushed on a phone-width screen
- [x] `DocumentSidebar` (Client) — nested tree with expand/collapse, create root/child, inline rename, up/down sibling reorder, archive (confirmed via `ConfirmDialog` - Stage 9), collapsible archived list (with an explicit "No archived documents" empty state - Stage 9) with restore; all mutation controls role-gated (VIEWER sees a read-only tree); icon-only controls carry `aria-label`s (Stage 9)
- [x] `/invitations/[token]` standalone accept/reject page (email-link flow)
- [x] `WorkspaceSwitcher` (top bar, links between workspaces, search trigger, responsive at ≤768px - Stage 9)
- [x] `proxy.ts` redirect-if-no-cookie for `/workspace/*`; also gates `/p/*` (Stage 7) to guarantee a real 404 status for unpublished/nonexistent slugs - see ADR-018
- [x] `/p/[slug]` public document page (Stage 7) - Server Component, no auth, SSR/ISR

### Frontend Visual Redesign
- [x] Full visual/UX redesign of every user-facing page — no backend logic, API contracts, or Yjs behavior changed. Design system: `app/globals.css` token layer (color incl. dark mode, spacing, radius, shadow, motion, presence palette), `Fraunces`/`Inter`/`IBM Plex Mono` via `next/font`. New shared primitives (`IconButton`, `Badge`, `Avatar`/`AvatarStack`, `Tabs`, `Tooltip`, `Menu`/`MenuItem`, `Skeleton`, `FormField`) and a hand-rolled icon set (`components/ui/icons.tsx`) replacing emoji glyphs throughout.
- [x] `WorkspaceShell` reorganized into tabs (General/Members/Invitations/Billing) via the new `Tabs` primitive; Invitations tab surfaces the previously-unused `listWorkspaceInvitations` endpoint (no backend change). `WorkspaceSwitcher`'s workspace list is now a `Menu` dropdown instead of a horizontal scrolling pill list; added an account menu (avatar + logout).
- [x] See `05-frontend.md`'s "UI" section for the full primitive/token inventory. This fixed a pre-existing broken working-tree state (`globals.css`/`Button`/`Card`/`Input` `.module.css` had been deleted by an earlier uncommitted session with no replacement) as a side effect of rebuilding those exact files.

### Frontend Completion & UX (Stage 9)
- [x] **Toast/feedback system**: `components/ui/ToastProvider.tsx` (`useToast()`), mounted once in the root layout. Used for document created, invitation sent, comment/reply added, version restored, publish/unpublish, attachment uploaded, link copied, and billing upgrade/downgrade - deliberately **not** used for routine background sync (polling, presence) or for errors that already have an established inline-error-text convention
- [x] **Shared modal/panel infrastructure**: `components/ui/SlideOverPanel.tsx` (Comments/Versions/Attachments now all share one implementation instead of three duplicated overlay/header/close-button blocks) and `components/ui/ConfirmDialog.tsx` (used for archive confirmation - archiving takes a document's entire subtree with it and was previously one click with no confirmation), both built on a new shared `lib/useFocusTrap.ts` hook (Tab-trapping + Escape-to-close) also adopted by `SearchDialog`. None of these existed before Stage 9 - every panel/dialog previously had its own ad hoc, focus-trap-free markup
- [x] **Richer plan-limit error messages**: `lib/api-error.ts` gained `formatPlanLimitMessage()`, which states the limit, current usage, and a concrete next step (e.g. "…(50/50 used on the FREE plan). Upgrade to PRO from the workspace settings page.") instead of the raw backend message alone - used for document-creation and invitation-accept limit errors
- [x] **Notification deep-linking**: clicking a notification now marks it read and navigates straight to `/workspace/:workspaceId/document/:documentId`, instead of only telling the user something happened with no way to go see it - required a small, additive backend change (`NotificationsService.list()` now joins `documents` to derive `workspaceId`; `NotificationResponseDto` gained the field) since `Notification` itself only ever stored `documentId`
- [x] **Editor loading/read-only clarity**: a distinct "Connecting to document…" spinner state while `status === 'connecting'` (previously showed a misleading "This document is empty" for a moment during hydration); the read-only banner is now visually distinct (icon + left accent border) and mentions comment ability when relevant; `PresenceBar` is `role="status" aria-live="polite"` so a disconnect/reconnect is announced
- [x] **Performance**: `VersionHistoryPanel`/`CommentsPanel`/`AttachmentsPanel`/`SearchDialog`/`BillingSection` are all loaded via `next/dynamic({ ssr: false })` - their code no longer ships in the document-page or top-nav bundle for sessions that never open them
- [x] Accessibility pass: icon-only buttons (tree row controls, remove-block, checkbox, notification items) all carry matching `aria-label`s; `role="alert"`/`role="status"` added to error/status text throughout; dialogs are proper `role="dialog"`/`role="alertdialog"` with `aria-modal`
- [x] Responsive pass at 375/768/1440px: the sidebar drawer (above), a compacted top nav (brand/labels hidden, icon-only search below 768px), `flex-wrap` added to every form/row that could previously overflow (member rows, invite form, workspace/invitation rows, archived banner, editor toolbar), and reduced padding/title size on the document page and public page below 480px
- [x] Two real bugs found and fixed (same class as ADR-020's Stage 8 finding): `NotificationsService.createIfNotExists`'s idempotency check had the identical `result.identifiers.length > 0` bug as `BillingService.applyEvent` - TypeORM's `orIgnore()` returns `[null]` (not `[]`) for a skipped duplicate row, so every redelivered notification job was logged/metriced as "created" instead of "duplicate" (the DB constraint still correctly prevented the duplicate row itself - this was a metrics/log correctness bug, not a data-correctness one)

### Testing (verified by running, not assumed)
- [x] Backend unit tests: **200 passing** (`npm test`) — permission matrix (incl. Stage 8's `canManageWorkspaceSettings`), password verification, refresh rotation/reuse, invitation expiration/email-matching, document tree/ordering/archive/restore/IDOR/publish/unpublish/slug-collision (Stage 7), `CollaborationService` session lifecycle/awareness/eviction cleanup, `CollaborationPersistenceService` hydrate/flush/throttle/search-index-update-on-durable-write (Stage 8), `yjs-document.util` decode/serialize/replace, `VersionsService` list/inspect/create/restore/manual-snapshot-gating (Stage 8), `CommentsService`/`NotificationsService`/`AttachmentsService` (Stage 6; Stage 9 added the `workspaceId`-join test for `list()` and fixed the duplicate-`[null]`-identifiers fixture), `PublicDocumentsService` (Stage 7), `EntitlementsService` (Stage 8), `BillingService` (Stage 8)
- [x] Backend e2e tests: **109 passing** (`npm run test:e2e`, real Postgres/Redis) — unchanged in count from Stage 8 (Stage 9 was a frontend-focused stage; the one backend change - the notifications `workspaceId` join - is covered by the existing Stage 6 comments/notifications/attachments e2e suite, which remains green)
- [x] Frontend tests: **79 passing** (`npm test`, vitest) — email/password validation, permission-matrix UI gating (incl. Stage 8's `canManageBilling`), invitation/document/collaboration/versions/comments/notifications/attachments/publish-unpublish/search/billing API call shapes, document tree-building, text-diff algorithm, `isSafeUrl`/`sanitizeUrl` (Stage 7), plus Stage 9's new `lib/api-error.test.ts` (`isPlanLimitError`/`formatPlanLimitMessage`)

### Final Hardening & Submission Audit (Stage 10)
- [x] Replaced the obsolete infrastructure-diagnostic `/` page with a responsive product landing page; authenticated users with a refresh cookie are routed to `/workspace`
- [x] Real browser verification completed for root, login, empty workspace, workspace/document creation, editor connection, comments/files/history/search empty states, FREE-plan snapshot rejection, publish/public render/unpublish, and 375/768/1440 layouts (no horizontal overflow)
- [x] Final checks: backend lint/build/200 unit/109 e2e; frontend lint/build/typecheck/79 tests
- [x] Fresh isolated Docker project verified with all five services healthy; found and fixed missing migration-on-container-start behavior, then confirmed all 8 migrations on a new volume
- [x] Security/architecture audit confirmed scoped guards/queries, centralized RBAC and entitlements, no active unsafe HTML rendering, no client-exposed secrets, one real BullMQ processor for the one application queue, and correct idempotent insert handling at both call sites
- [x] Health/liveness/metrics verified live; Swagger title/description/version and README updated from stale Stage 2 claims

### Final TT Gap Closure (post-Stage-10)
- [x] **Document-level ACL**: `document_collaborators` table (`documentId`, `userId`, `accessLevel: VIEWER|EDITOR`) + `documents.restricted` boolean, resolved by `DocumentPermissionsService` on top of the workspace role, enforced identically over REST (`DocumentsController`) and the `/collab` gateway's `join` handler. New endpoints: `GET/POST .../collaborators`, `DELETE .../collaborators/:userId`, `PATCH .../access`, all OWNER/ADMIN-gated (`canManageDocumentAccess`). `list()`/`search()` filtered through `filterVisible` so a restricted document never leaks by title/snippet. See ADR-022.
- [x] **Public edit-by-link + expiry**: `documents.publicAccessMode` (`view`/`edit`) and `publicExpiresAt` extend the Stage 7 publish fields; an expired link 404s identically to an unpublished one. Anonymous collaborative editing via a new `join-public {slug}` `/collab` event (`CollaborationGateway.handlePublicJoin`), scoped to exactly one document, no workspace/membership data reachable from that session. Frontend: `PublishControl` gained mode/expiry selects; `/p/[slug]` renders the new `PublicCollaborativeEditor` (reuses `BlockView` on top of `usePublicCollaboration`) instead of the read-only view when `mode === 'edit'`. See ADR-023.
- [x] **Sidebar drag-and-drop**: `@dnd-kit/core`-based reorder/reparent/move-to-root on the document tree, zone-based drop targets (before/inside/after), optimistic update with rollback on failure, reusing the existing move endpoint unchanged — as ADR-012 predicted, no backend change was needed. Up/down buttons kept in each row's overflow menu as a non-pointer-drag fallback. See ADR-027.
- [x] **Streaming SSR**: `/p/[slug]` split into an instant static shell + a nested `async` Server Component wrapped in `<Suspense>` for the backend-fetched content, with a `Skeleton`-based fallback; confirmed genuinely multi-chunk via curl (initial response contains the fallback + a React Suspense placeholder comment, a later chunk resolves it) rather than a client-side spinner.
- [x] **Server Actions**: `inviteMemberAction`/`createWorkspaceAction` (`features/workspaces/actions.ts`, `"use server"`), driven by `useActionState`, taking the caller's access token as a bound argument rather than the action independently exchanging the refresh-token cookie (would race the client's own refresh cycle against the backend's reuse-detection). `createWorkspaceAction` `redirect()`s server-side on success. See ADR-028.
- [x] **Async search indexing**: `CollaborationPersistenceService.flush()` now enqueues onto the previously-processor-less `QueueName.SEARCH_INDEX` BullMQ queue instead of writing `documents.contentText` inline; new `SearchIndexProcessor` re-reads the durable buffer itself (idempotent/duplicate-safe by construction, no dedupe key needed), 3 retries with exponential backoff. Verified with a timing-sensitive e2e test (not searchable immediately after edit, becomes searchable after the queue processes). See ADR-024.
- [x] **Redis document-tree read cache**: `DocumentsService.list()` caches the full per-workspace document set (`doc-tree:<workspaceId>`, 60s TTL safety net) *before* permission filtering, which still runs on every read regardless of cache hit/miss — a cached entry can never leak a restricted document. Explicit invalidation on create/rename/move/archive/restore/publish/unpublish/restrict; fail-open on a Redis error (falls through to Postgres, never a broken response). See ADR-025.
- [x] **Dev/staging config profiles**: `NODE_ENV=staging` is a fourth valid value, treated as production-like for every security-sensitive check (secure cookies, hidden error detail, no dev-token exposure, structured logs) via one `AppConfigService.isProductionLike` getter — fixes what would otherwise have been a real gap (staging silently inheriting "not production" convenience behavior). `.env.staging.example` documents only the deltas from `.env.example`; README gained an "Environments" section (dev/test/staging/production). See ADR-026.
- [x] Verified against a freshly rebuilt Docker stack: backend 221 unit / 122 e2e (1 known-flaky timing test, confirmed flaky on the unmodified codebase too under full-suite load — not a regression), frontend 93 vitest tests, all lint/typecheck/build green. Browser automation was unavailable in this environment (same limitation as Stages 6-10) — the two Server Actions and the DnD pointer-drag gesture specifically could not be exercised in a real browser, verified by code review + successful build only; everything else was verified live via curl/socket.io-client e2e tests against the rebuilt stack.

### Submission Regression Repair
- [x] Local Docker auth no longer forces the backend's security profile to `production`: Compose now preserves `.env`'s `NODE_ENV`, so the default HTTP localhost profile emits a usable host-only `SameSite=Lax` refresh cookie while staging/production remain `Secure`. Live verification covered register/login, rotation, session refresh after `docker compose restart`, logout/login, stable secrets, and persisted database data.
- [x] Tree DnD resolves before/inside/after from the live pointer position on every drag frame (rather than the translated source-row center only when the collided row changes), suppresses adjacent no-op moves, and resyncs after API rollback. Resolver tests cover root reorder, reparenting, root move, self/descendant rejection, and nested ordering; live API verification confirmed server order survives refresh/restart. Browser automation remained unavailable, so the pointer gesture itself was not browser-driven.

## Not Implemented

- Arrow-key navigation *between* editor blocks (Tab already moves focus between block textareas in DOM order, which satisfies "the editor must remain keyboard usable" without touching CRDT-adjacent block-focus logic) - unrelated to the now-implemented sidebar drag-and-drop, which was a different, tree-level reordering concern (see ADR-012/027)
- A per-document unread-comment-count badge on the editor's "Comments" toolbar button (Stage 9) - would need an extra fetch per document-page load just for a badge; the existing global notification bell already surfaces new mentions/replies/resolves
- A real upload progress bar for attachments (Stage 9) - the existing spinner-only feedback was kept; a true byte-progress indicator needs `XMLHttpRequest` in place of `fetch` for the presigned PUT, judged not worth the added complexity for this project's scale
- Drag-and-drop file upload for attachments (Stage 9) - file-input-only, matching the "don't over-design" instruction for this stage
- Real Stripe (or any other real payment provider) integration — Stage 8 shipped a mock/dev billing flow behind a replaceable `PaymentProvider` boundary, since real payment integration wasn't required (see ADR-020)
- A third billing tier beyond FREE/PRO
- Search over comments, version history, or attachment metadata (Stage 8 explicitly scoped to document title + content only)
- Publishing captures only "current title + blocks" - no per-publication custom OG image, no author byline, no published-content diffing/history (a published page has no relation to Stage 5's version history)
- Published attachments/images hosted in MinIO - the block model's `image` type is still a raw external `imageUrl` string (Stage 4 limitation, unchanged), so there's no "publish this workspace's MinIO-hosted attachment publicly" flow; not built since no block currently references a Stage 6 `Attachment` entity to begin with
- A lightweight existence-only check endpoint for `proxy.ts`'s pre-render 404 gate (Stage 7) - it currently reuses the full content endpoint, which is simple but technically against Next's own "keep proxy checks fast" guidance; acceptable at this project's scale, worth revisiting if public traffic grows
- Real email/push delivery for notifications — in-app only (persistent `Notification` rows + polling), per explicit Stage 6 scope
- Yjs-relative-position comment anchoring to specific text ranges — comments reference `documentId` only; not required by any concrete Stage 6 requirement, deliberately not built speculatively
- Comment threading beyond two levels (root + flat replies only, no reply-to-reply)
- Ownership transfer
- Position rebalancing (fractional positions never renumbered; not a practical concern at Stage 3 scale — see ADR-011)
- Real email delivery (invitations only work via dev-mode token exposure or in-app by-id accept)
- Full multi-step Yjs sync-protocol negotiation (`y-protocols/sync` step1/step2) - still uses a simplified always-send-full-state-on-join bootstrap, correct but not bandwidth-optimal for very large documents (see ADR-013)
- Append-only update log + compaction for durability - Stage 5 chose "one upserted full-state row per document" instead (simpler, no replay-time growth); a real high-write-volume system might prefer the log+compaction approach (see ADR-014)
- Git-like branching/diffing of versions - restore is whole-document only, no partial/selective restore
- A real persistence/eviction strategy bound to memory pressure - eviction is time-based (grace period) only, not memory-aware
